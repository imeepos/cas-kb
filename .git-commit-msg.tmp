feat(hybrid): M6-B 混合检索内核——repo.SearchHybrid 落地 RRF(k=60 固定常数)融合 BM25 与向量余弦两路排名(各取前 50),score=Σ1/(60+rank),输出融合分降序、平局路径升序;查询词经 Embedder 恰好 1 次嵌入(30s 上限),失败响亮报错不降级;快照无 vec / 模型不一致给 rebuild --embed 可行动指引(哨兵错误供 serve 映射 409);cosineRank 对快照 vec 分桶平扫(桶号/路径升序,累加顺序固定,零向量约定相似 0)。search.go 抽出 snapshotFor 供词法/混合共用(--at 语义一致);embed 包拆出 FromEnvWithNext/NotConfiguredError + NextHybridSearch 检索场景指引(CLI 与 serve 共用一份文案);view.SearchRow 增可选 mode 字段(omitempty,仅 --hybrid 时存在)+ WithMode;cmdSearch 增 --hybrid;serve 读 KB_EMBED_*(未配置不拦启动,mode=hybrid 按请求 409+配置指引);/api/v1/search 增 mode 参数(缺省=纯词法,非法取值 400,hybrid 失败语义 409)。缺省调用零变化,现有测试全绿。
